/** No browser tab to name. See `document-title.web.ts` for the half that does something. */
export function useDocumentTitle(_title: string | null): void {
  // Native shows the screen's own header, which the app draws itself.
}
